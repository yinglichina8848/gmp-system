# EDMS子系统（电子文档管理系统）详细需求分析

> **重要更新**: 原独立的File服务已完全集成到EDMS中，以下需求包含完整的文件存储管理功能。

## 1. 文件存储管理模块 (File Storage Management) - 新增

### 1.1 功能需求

#### 1.1.1 基础文件操作
- **文件上传**：支持单文件和批量文件上传
- **文件下载**：支持流式下载和批量下载
- **文件删除**：支持软删除和批量删除
- **文件重命名**：支持文件名修改
- **文件移动**：支持文件在不同目录间移动
- **文件复制**：支持文件复制功能

#### 1.1.2 大文件处理
- **分块上传**：支持大文件分块上传，提高上传成功率
- **断点续传**：上传中断后可从断点继续上传
- **并发下载**：支持多线程并发下载大文件
- **压缩传输**：自动压缩大文件以提高传输效率

#### 1.1.3 文件格式支持
- **文档格式**：PDF, DOC, DOCX, XLS, XLSX, PPT, PPTX, TXT
- **图片格式**：JPG, PNG, GIF, BMP, TIFF
- **视频格式**：MP4, AVI, MOV, WMV
- **音频格式**：MP3, WAV, AAC
- **压缩格式**：ZIP, RAR, 7Z, TAR.GZ

#### 1.1.4 文件预览
- **在线预览**：PDF、图片、文本文件在线预览
- **缩略图生成**：自动生成图片和PDF的缩略图
- **预览权限控制**：根据用户权限控制预览功能

### 1.2 数据模型

#### 1.2.1 通用文件表 (common_file)
```sql
CREATE TABLE common_file (
    id BIGSERIAL PRIMARY KEY,
    file_name VARCHAR(255) NOT NULL,
    file_type VARCHAR(100), -- 文件MIME类型
    file_size BIGINT NOT NULL,
    file_path VARCHAR(500) NOT NULL, -- MinIO中的对象路径
    checksum VARCHAR(128) NOT NULL, -- SHA256校验和
    bucket_name VARCHAR(100), -- MinIO桶名称
    module VARCHAR(50), -- 所属模块(edms, qms等)
    created_by VARCHAR(100),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    metadata JSONB, -- 文件元数据
    status VARCHAR(20) DEFAULT 'ACTIVE', -- ACTIVE, DELETED, ARCHIVED
    version INTEGER DEFAULT 1
);
```

#### 1.2.2 文件访问日志表 (file_access_log)
```sql
CREATE TABLE file_access_log (
    id BIGSERIAL PRIMARY KEY,
    file_id BIGINT REFERENCES common_file(id),
    user_id VARCHAR(100),
    action VARCHAR(50), -- UPLOAD, DOWNLOAD, DELETE, VIEW
    ip_address INET,
    user_agent TEXT,
    access_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    result VARCHAR(20), -- SUCCESS, FAILED
    error_message TEXT
);
```

## 2. 文档管理模块 (Document Management)

### 1.1 功能需求

#### 1.1.1 文档创建与编辑
- **文档模板选择**：系统提供预定义的文档模板（SOP、规范、记录等）
- **所见即所得编辑**：支持富文本编辑，包括表格、图片、列表等
- **草稿保存**：自动保存草稿功能，防止数据丢失
- **版本控制**：新版本创建时自动备份上一版本
- **元数据管理**：自动提取或手动录入文档属性（标题、作者、部门、文件类型等）

#### 1.1.2 文档存储与组织
- **层级分类**：支持多级文档分类体系
- **标签管理**：支持标签标记，便于快速检索
- **全文索引**：对文档内容进行全文索引，支持关键词搜索
- **权限控制**：基于角色和部门的文档访问权限

### 1.2 数据模型

#### 1.2.1 文档表 (documents)
```sql
CREATE TABLE documents (
    id BIGSERIAL PRIMARY KEY,
    document_number VARCHAR(50) UNIQUE NOT NULL, -- DOC-YYYY-MMDD-001
    title VARCHAR(200) NOT NULL,
    document_type VARCHAR(50) NOT NULL, -- SOP, SPECIFICATION, RECORD, MANUAL
    category VARCHAR(100), -- 分类路径，用/分隔
    status VARCHAR(20) DEFAULT 'DRAFT', -- DRAFT, IN_REVIEW, APPROVED, EFFECTIVE, WITHDRAWN
    current_version VARCHAR(20) DEFAULT '1.0',
    template_id BIGINT, -- 关联的模板ID
    owner_department VARCHAR(100),
    author VARCHAR(100) NOT NULL,
    approver VARCHAR(100),
    effective_date TIMESTAMP,
    expiry_date TIMESTAMP,
    retention_period INTEGER, -- 保留期(月)
    confidentiality_level VARCHAR(20) DEFAULT 'NORMAL', -- NORMAL, CONFIDENTIAL, RESTRICTED
    file_path VARCHAR(500), -- 存储路径
    file_size BIGINT,
    content_type VARCHAR(100), -- MIME类型
    checksum VARCHAR(128), -- SHA256校验和
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 1.2.2 文档版本表 (document_versions)
```sql
CREATE TABLE document_versions (
    id BIGSERIAL PRIMARY KEY,
    document_id BIGINT REFERENCES documents(id) ON DELETE CASCADE,
    version_number VARCHAR(20) NOT NULL,
    version_type VARCHAR(20) DEFAULT 'REVISION', -- REVISION, MAJOR, MINOR
    change_reason TEXT, -- 变更原因说明
    change_summary TEXT, -- 变更内容摘要
    author VARCHAR(100) NOT NULL,
    reviewer VARCHAR(100),
    approver VARCHAR(100),
    approval_date TIMESTAMP,
    file_path VARCHAR(500),
    file_size BIGINT,
    checksum VARCHAR(128),
    is_current BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

## 3. 审批工作流模块 (Approval Workflow)

### 2.1 功能需求

#### 2.1.1 流程配置
- **可视化流程设计**：拖拽式流程设计器
- **节点类型**：支持串行审批、并行审批、条件分支
- **角色绑定**：将审批节点绑定到具体角色或人员
- **时间管控**：设置审批时限和自动提醒

#### 2.1.2 审批处理
- **任务分派**：系统自动根据流程规则分派审批任务
- **审批意见**：支持填写审批意见和修改建议
- **电子签名**：审批时自动附加电子签名
- **超时处理**：超期自动升级或转发

### 2.2 数据模型

#### 2.2.1 审批流程表 (approval_workflows)
```sql
CREATE TABLE approval_workflows (
    id BIGSERIAL PRIMARY KEY,
    workflow_code VARCHAR(50) UNIQUE NOT NULL,
    workflow_name VARCHAR(200) NOT NULL,
    document_type VARCHAR(50), -- 适用的文档类型
    workflow_definition TEXT, -- JSON格式的工作流定义
    version VARCHAR(20) DEFAULT '1.0',
    is_active BOOLEAN DEFAULT TRUE,
    created_by VARCHAR(100),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 2.2.2 审批实例表 (approval_instances)
```sql
CREATE TABLE approval_instances (
    id BIGSERIAL PRIMARY KEY,
    workflow_id BIGINT REFERENCES approval_workflows(id),
    document_id BIGINT REFERENCES documents(id),
    initiator VARCHAR(100) NOT NULL, -- 发起人
    current_step VARCHAR(50), -- 当前步骤名称
    status VARCHAR(20) DEFAULT 'IN_PROGRESS', -- IN_PROGRESS, APPROVED, REJECTED, WITHDRAWN
    priority VARCHAR(20) DEFAULT 'NORMAL',
    started_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    completed_at TIMESTAMP,
    deadline TIMESTAMP, -- 完成截止时间
    comments TEXT, -- 整体审批意见
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

## 3. 检索与访问模块 (Search & Access)

### 3.1 功能需求

#### 3.1.1 多维度检索
- **基础检索**：按标题、编号、作者、日期等字段检索
- **内容检索**：全文内容搜索，支持关键词高亮
- **高级检索**：组合多个检索条件，支持布尔逻辑
- **分类浏览**：按分类体系进行浏览导航

#### 3.1.2 访问控制
- **权限验证**：访问时动态验证用户权限
- **审计记录**：记录所有访问行为和操作痕迹
- **离线访问**：重要文档支持离线下载和查看（权限控制）
- **过期提醒**：文档过期或即将过期时提前提醒

### 3.2 数据模型

#### 3.2.1 文档访问记录表 (document_access_logs)
```sql
CREATE TABLE document_access_logs (
    id BIGSERIAL PRIMARY KEY,
    document_id BIGINT REFERENCES documents(id),
    document_version_id BIGINT REFERENCES document_versions(id),
    user_id VARCHAR(100) NOT NULL,
    user_name VARCHAR(200),
    user_department VARCHAR(100),
    access_type VARCHAR(20) NOT NULL, -- VIEW, DOWNLOAD, PRINT, EDIT, DELETE
    access_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    session_id VARCHAR(50), -- 会话标识
    ip_address VARCHAR(45), -- IPv4/IPv6地址
    user_agent TEXT, -- 浏览器信息
    success BOOLEAN DEFAULT TRUE, -- 是否成功
    error_message VARCHAR(500) -- 错误信息
);
```

## 4. 版本控制与合规模块 (Version Control & Compliance)

### 4.1 功能需求

#### 4.1.1 版本管控
- **自动版本号**：新增版本自动计算版本号（major.minor.revision）
- **版本比较**：可视化显示版本差异
- **版本冻结**：重要文档版本可以冻结防止意外修改
- **历史追溯**：可查看任意历史版本的完整信息

#### 4.1.2 合规性保障
- **审计追踪**：所有文档操作都有完整的审计日志
- **电子签名**：关键操作要求电子签名验证身份
- **数据完整性**：防止文档内容被篡改，数字签名保护
- **生命周期管理**：文档从创建到销毁的完整生命周期追踪

### 3.2 数据模型

#### 3.2.1 电子签名记录表 (electronic_signatures)
```sql
CREATE TABLE electronic_signatures (
    id BIGSERIAL PRIMARY KEY,
    document_id BIGINT REFERENCES documents(id),
    document_version_id BIGINT REFERENCES document_versions(id),
    signer_id VARCHAR(100) NOT NULL, -- 签署人ID
    signer_name VARCHAR(200),
    signer_role VARCHAR(100),
    signature_type VARCHAR(20) NOT NULL, -- APPROVE, REVIEW, CREATE, WITHDRAW
    signature_data TEXT, -- 签名数据（JSON格式）
    signature_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    ip_address VARCHAR(45),
    certificate_info TEXT, -- 证书信息JSON
    validity_status VARCHAR(20) DEFAULT 'VALID', -- VALID, INVALID, EXPIRED
    verification_result BOOLEAN DEFAULT TRUE
);
```

#### 3.2.2 文档合规记录表 (document_compliance_logs)
```sql
CREATE TABLE document_compliance_logs (
    id BIGSERIAL PRIMARY KEY,
    document_id BIGINT REFERENCES documents(id),
    compliance_type VARCHAR(50) NOT NULL, -- GMP_REQUIREMENT, COMPANY_POLICY, REGULATORY
    compliance_standard VARCHAR(200),
    check_result VARCHAR(20) NOT NULL, -- COMPLIANT, NON_COMPLIANT, NOT_APPLICABLE
    checked_by VARCHAR(100),
    checked_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    notes TEXT,
    corrective_action TEXT, -- 纠正措施
    follow_up_date TIMESTAMP -- 后续跟进日期
);
```

## 5. REST API接口设计

### 5.1 文档管理接口

#### 5.1.1 创建文档
```
POST /api/edms/documents
Content-Type: multipart/form-data

{
    title: "新的SOP文档",
    documentType: "SOP",
    category: "生产管理/质量控制",
    content: "<富文本内容>",
    attachments: [file1, file2]
}
```

#### 5.1.2 文档检索
```
GET /api/edms/documents/search?query=质量控制&page=0&size=20&sort=updatedAt,desc
```

### 5.2 审批流程接口

#### 5.2.1 提交审批
```
POST /api/edms/documents/{id}/submit-approval
{
    "workflowCode": "SOP_APPROVAL",
    "priority": "NORMAL",
    "comments": "请审批新的质量控制SOP"
}
```

#### 5.2.2 执行审批
```
POST /api/edms/approvals/{instanceId}/step/{stepId}/action
{
    "action": "APPROVE",
    "comments": "内容完整，同意发布",
    "signature": "数字签名数据"
}
```

### 5.3 数据验证规则

#### 5.3.1 文档验证
- 文件大小限制（最大100MB）
- 文件类型支持（PDF, DOC, DOCX, XLS, XLSX等）
- 必填字段校验（标题、文档类型、作者等）
- 权限验证（创建者必须有对应权限）
- 唯一性校验（文档编号全局唯一）

#### 5.3.2 审批验证
- 用户权限校验（必须是审批流程中的节点负责人）
- 操作时效校验（在有效时间内操作）
- 重复操作防护（防止重复审批）

## 6. 数据库设计要点

### 6.1 索引优化
- 文档状态和类型联合索引
- 全文搜索索引（使用PostgreSQL的全文搜索功能）
- 时间范围查询索引（创建时间、审批时间等）
- 用户权限索引（基于用户和文档的访问控制）

### 6.2 数据完整性
- 外键约束：确保文档版本、审批实例等关联关系正确
- 枚举约束：状态、类型等字段使用预定义枚举值
- 数据级联：重要数据的级联删除和更新策略

### 6.3 性能优化
- **缓存策略**：热点文档内容缓存，权限信息缓存
- **异步处理**：大文件上传、复杂的全文索引建立使用异步处理
- **读写分离**：可能的话实现数据库读写分离

## 2. 中药特色文档管理模块 (TCM Document Management)

### 2.1 功能需求

#### 2.1.1 中药材管理文档
- **产地认证文档**：管理中药材产地认证相关文档
- **采收加工文档**：记录中药材采收和初加工标准文档
- **仓储养护文档**：中药材仓储和养护管理文档
- **质量标准文档**：中药材质量标准和检验方法文档

#### 2.1.2 中药炮制工艺文档
- **炮制标准文档**：各类中药材炮制标准操作程序
- **辅料管理文档**：炮制辅料使用标准和管理文档
- **设备操作文档**：炮制设备标准操作和维护文档
- **质量控制文档**：炮制过程质量控制文档

#### 2.1.3 中药提取工艺文档
- **提取方法文档**：各种提取方法的标准操作程序
- **工艺参数文档**：提取工艺参数控制文档
- **中间体文档**：提取中间体质量标准文档
- **设备验证文档**：提取设备验证和确认文档

#### 2.1.4 中药特色文档模板
- **中药材管理模板**：中药材采购、验收、存储等管理文档模板
- **炮制工艺模板**：炮制标准操作程序模板
- **提取工艺模板**：提取标准操作程序模板
- **质量控制模板**：中药特色质量控制文档模板

### 2.2 数据模型

#### 2.2.1 中药文档分类表 (tcm_document_categories)
```sql
CREATE TABLE tcm_document_categories (
    id BIGSERIAL PRIMARY KEY,
    category_name VARCHAR(100) NOT NULL, -- 分类名称
    category_code VARCHAR(50) UNIQUE NOT NULL, -- 分类编码
    parent_id BIGINT, -- 父分类ID
    level INTEGER NOT NULL, -- 分类层级
    description TEXT, -- 分类描述
    is_active BOOLEAN DEFAULT TRUE, -- 是否启用
    sort_order INTEGER DEFAULT 0, -- 排序
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 2.2.2 中药文档属性表 (tcm_document_attributes)
```sql
CREATE TABLE tcm_document_attributes (
    id BIGSERIAL PRIMARY KEY,
    document_id BIGINT REFERENCES documents(id),
    herb_name VARCHAR(100), -- 药材名称
    herb_origin VARCHAR(200), -- 药材产地
    processing_method VARCHAR(100), -- 加工方法
    炮制方法 VARCHAR(100), -- 炮制方法
    extraction_method VARCHAR(100), -- 提取方法
    quality_standard VARCHAR(200), -- 质量标准
    special_attributes JSONB, -- 特殊属性（JSON格式存储）
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 2.3 API接口设计

#### 2.3.1 中药文档分类管理接口
```
POST /api/edms/tcm/categories
Content-Type: application/json

Request Body:
{
    "categoryName": "中药材管理",
    "categoryCode": "TCM_HERB_MANAGEMENT",
    "parentId": 0,
    "level": 1,
    "description": "中药材采购、验收、存储等管理文档"
}

Response:
{
    "id": 20001,
    "categoryName": "中药材管理",
    "categoryCode": "TCM_HERB_MANAGEMENT",
    "level": 1,
    "isActive": true,
    "createdAt": "2025-11-27T10:00:00Z"
}
```

#### 2.3.2 中药特色文档检索接口
```
GET /api/edms/tcm/documents?herbName=人参&processingMethod=蒸制&page=1&size=20

Response:
{
    "content": [
        {
            "id": 10002,
            "documentNumber": "SOP-20251127-002",
            "title": "人参蒸制标准操作程序",
            "documentType": "SOP",
            "category": "中药管理/炮制工艺",
            "status": "APPROVED",
            "currentVersion": "1.0",
            "author": "王五",
            "effectiveDate": "2025-11-28T00:00:00Z",
            "tcmAttributes": {
                "herbName": "人参",
                "processingMethod": "蒸制",
                "qualityStandard": "《中国药典》2020版"
            }
        }
    ],
    "totalElements": 1,
    "totalPages": 1,
    "currentPage": 1
}
```

## 5. 文件存储与管理模块 (File Storage & Management)

### 5.1 功能需求

#### 5.1.1 文件上传与存储
- **单文件上传**：支持单个文件上传，文件大小限制为5GB
- **批量文件上传**：支持多文件批量上传，最多支持100个文件同时上传
- **分片上传**：支持大文件分片上传，断点续传功能
- **文件格式验证**：支持PDF、Word、Excel、图片、视频等常见格式
- **文件完整性校验**：使用SHA256算法生成文件校验和，确保文件完整性
- **存储位置管理**：智能分配存储位置，支持分布式存储
- **中药文件管理**：中药材图片、检验图谱等专项文件管理

#### 5.1.2 文件下载与访问
- **直接下载**：支持单个文件直接下载
- **批量下载**：支持多文件打包下载（ZIP格式）
- **临时下载链接**：生成带过期时间的临时下载链接
- **流式下载**：支持大文件流式下载，节省内存
- **下载权限控制**：基于用户角色和文件权限控制下载访问

#### 5.1.3 文件预览与转换
- **在线预览**：支持PDF、Word、Excel、图片等格式在线预览
- **文档转换**：支持文档格式转换（Word转PDF等）
- **缩略图生成**：为图片和文档自动生成缩略图
- **分页预览**：支持多页文档的分页预览

#### 5.1.4 文件组织与分类
- **文件夹管理**：支持多级文件夹结构创建和管理
- **文件标签**：支持为文件添加多个标签，便于分类检索
- **文件元数据**：丰富的文件元数据管理（文件名、大小、类型、创建时间等）
- **智能分类**：基于文件内容和特征的自动分类建议
- **中药文件分类**：中药材图片、检验图谱等专项文件分类

### 5.2 数据模型

#### 5.2.1 通用文件表 (common_files)
```sql
CREATE TABLE common_files (
    id BIGSERIAL PRIMARY KEY,
    file_name VARCHAR(500) NOT NULL, -- 原始文件名
    file_path VARCHAR(1000) NOT NULL, -- 存储路径
    file_size BIGINT NOT NULL, -- 文件大小(字节)
    content_type VARCHAR(200), -- MIME类型
    file_hash VARCHAR(128) NOT NULL, -- SHA256校验和
    bucket_name VARCHAR(100), -- 存储桶名称
    object_key VARCHAR(500), -- 对象键
    storage_type VARCHAR(50) DEFAULT 'MINIO', -- MINIO, S3等
    upload_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    uploader_id VARCHAR(100), -- 上传者ID
    uploader_name VARCHAR(200), -- 上传者姓名
    uploader_department VARCHAR(200), -- 上传者部门
    download_count INTEGER DEFAULT 0, -- 下载次数
    last_download_time TIMESTAMP, -- 最后下载时间
    is_deleted BOOLEAN DEFAULT FALSE, -- 逻辑删除标记
    deleted_time TIMESTAMP, -- 删除时间
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 5.2.2 文件上传记录表 (file_upload_logs)
```sql
CREATE TABLE file_upload_logs (
    id BIGSERIAL PRIMARY KEY,
    file_id BIGINT REFERENCES common_files(id),
    upload_session_id VARCHAR(100), -- 上传会话ID
    chunk_index INTEGER, -- 分片索引
    chunk_size BIGINT, -- 分片大小
    total_chunks INTEGER, -- 总分片数
    upload_status VARCHAR(50) DEFAULT 'IN_PROGRESS', -- IN_PROGRESS, COMPLETED, FAILED
    error_message TEXT, -- 错误信息
    upload_start_time TIMESTAMP,
    upload_end_time TIMESTAMP,
    ip_address VARCHAR(45), -- 上传IP
    user_agent TEXT, -- 用户代理
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 5.3 API接口设计

#### 5.3.1 文件上传接口
```
POST /api/edms/files/upload
Content-Type: multipart/form-data

Form Data:
- file: 文件数据
- metadata: 文件元数据JSON
- tags: 文件标签数组

Response:
{
    "id": 10001,
    "fileName": "document.pdf",
    "filePath": "/files/2025/11/27/document.pdf",
    "fileSize": 2048000,
    "contentType": "application/pdf",
    "uploadTime": "2025-11-27T10:00:00Z",
    "uploader": "张三"
}
```

#### 5.3.2 分片上传接口
```
POST /api/edms/files/upload/chunk
Content-Type: multipart/form-data

Form Data:
- file: 文件分片数据
- sessionId: 上传会话ID
- chunkIndex: 分片索引
- totalChunks: 总分片数

Response:
{
    "chunkIndex": 1,
    "uploadStatus": "SUCCESS",
    "message": "分片上传成功"
}
```

#### 5.3.3 文件下载接口
```
GET /api/edms/files/{id}/download

Headers:
- Range: bytes=0-1023 (可选，支持断点续传)

Response: 文件流
```

#### 5.3.4 文件预览接口
```
GET /api/edms/files/{id}/preview

Query Parameters:
- width: 预览宽度
- height: 预览高度
- page: 页码(多页文档)

Response: 预览图片或PDF数据
```

#### 5.3.5 中药特色文件检索接口
```
GET /api/edms/files/tcm?herbName=人参&type=image&page=1&size=20

Response:
{
    "content": [
        {
            "id": 20001,
            "fileName": "人参样品图.jpg",
            "filePath": "/files/tcm/herb_images/人参样品图.jpg",
            "fileSize": 1024000,
            "contentType": "image/jpeg",
            "uploadTime": "2025-11-27T09:30:00Z",
            "uploader": "检验员",
            "tcmMetadata": {
                "herbName": "人参",
                "origin": "吉林长白山",
                "harvestDate": "2025-10-15",
                "grade": "特级"
            }
        }
    ],
    "totalElements": 1,
    "totalPages": 1,
    "currentPage": 1
}
```

### 5.4 存储架构设计

#### 5.4.1 MinIO集成
```java
@Configuration
public class MinioConfiguration {

    @Bean
    public MinioClient minioClient() {
        return MinioClient.builder()
            .endpoint(minioProperties.getEndpoint())
            .credentials(minioProperties.getAccessKey(), minioProperties.getSecretKey())
            .build();
    }
}
```

#### 5.4.2 文件存储服务
```java
@Service
public class CommonFileService {

    @Autowired
    private MinioClient minioClient;

    @Autowired
    private CommonFileRepository fileRepository;

    // 文件上传
    public CommonFileDTO uploadFile(MultipartFile file, String uploaderId) {
        // 1. 文件验证
        validateFile(file);

        // 2. 生成存储路径和对象键
        String objectKey = generateObjectKey(file.getOriginalFilename());

        // 3. 上传到MinIO
        uploadToMinio(file, objectKey);

        // 4. 保存元数据到数据库
        CommonFile commonFile = saveFileMetadata(file, objectKey, uploaderId);

        // 5. 返回DTO
        return convertToDTO(commonFile);
    }

    // 文件下载
    public InputStream downloadFile(Long fileId) {
        CommonFile file = fileRepository.findById(fileId)
            .orElseThrow(() -> new FileNotFoundException("File not found"));

        // 从MinIO获取文件流
        return minioClient.getObject(
            GetObjectArgs.builder()
                .bucket(file.getBucketName())
                .object(file.getObjectKey())
                .build()
        );
    }
}
```

### 5.5 性能与安全考虑

#### 5.5.1 性能优化
- **分片上传**：大文件分片上传，提升上传成功率
- **并发控制**：限制同时上传文件的数量
- **缓存策略**：缓存热点文件元数据
- **CDN加速**：可选的CDN加速下载

#### 5.5.2 安全保障
- **文件扫描**：上传时进行病毒扫描
- **访问控制**：严格的文件访问权限控制
- **加密存储**：敏感文件加密存储
- **审计日志**：完整的文件操作审计记录

---

## 6. 系统管理模块 (System Management)

### 6.1 功能需求

#### 6.1.1 用户与权限管理
- **角色管理**：系统角色定义和管理
- **权限分配**：基于角色的权限分配
- **用户组管理**：用户组定义和管理
- **中药专业角色**：中药材管理、炮制、检验等专业角色

#### 6.1.2 系统配置管理
- **基础配置**：系统基础参数配置
- **模板管理**：文档模板管理
- **分类管理**：文档分类管理
- **中药特色配置**：中药材文档分类和模板专项配置

## 7. 附录

### 7.1 参考文档
- GMP法规
- FDA 21 CFR Part 11
- 《中国药典》
- 中药材生产质量管理规范(GAP)
- 中药材检验子系统需求文档

### 7.2 版本历史
| 版本 | 修改日期 | 修改内容 | 修改人 |
|------|---------|---------|--------|
| v1.0.0 | 2024-01-15 | 初始版本 | EDMS需求分析团队 |
| v2.0.0 | 2025-11-27 | 增加中药特色文档管理功能 | 详细需求分析团队 |

**文档信息：**
- 版本：v2.0 (中药特色增强版)
- 更新日期：2025年11月
- 编制人员：详细需求分析团队
- 状态：Draft
